<Page x:Class="Restatify.SupportChat.Presentation.LogsPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:utu="using:Uno.Toolkit.UI"
	  mc:Ignorable="d"
	  Background="{ThemeResource BackgroundBrush}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<utu:NavigationBar Content="{Binding Title}">
			<utu:NavigationBar.MainCommand>
				<AppBarButton Icon="Back" Click="OnBackClick" />
			</utu:NavigationBar.MainCommand>
		</utu:NavigationBar>

		<Grid Grid.Row="1" Padding="24" RowSpacing="12">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<Grid Grid.Row="0" ColumnSpacing="12">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<TextBlock Grid.Column="0" Text="{Binding Status}" TextWrapping="Wrap" VerticalAlignment="Center" />
				<Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Padding="12,8" />
			</Grid>

			<Border Grid.Row="1" Background="{ThemeResource SurfaceVariantColor}" CornerRadius="8" Padding="12">
				<TextBlock Text="{Binding LogFilePath}" TextWrapping="Wrap" Opacity="0.8" />
			</Border>

			<Border Grid.Row="2"
					Background="{ThemeResource SurfaceBrush}"
					BorderBrush="{ThemeResource OutlineColor}"
					BorderThickness="1"
					CornerRadius="8"
					Padding="12">
				<ScrollViewer VerticalScrollBarVisibility="Auto" VerticalScrollMode="Auto">
					<TextBlock Text="{Binding LogContent}"
							   TextWrapping="NoWrap"
							   FontFamily="Consolas"
							   FontSize="12" />
				</ScrollViewer>
			</Border>
		</Grid>
	</Grid>
</Page>
