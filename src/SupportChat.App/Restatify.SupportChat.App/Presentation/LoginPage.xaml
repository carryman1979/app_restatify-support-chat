<Page x:Class="Restatify.SupportChat.Presentation.LoginPage"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:Restatify.SupportChat.Presentation"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:utu="using:Uno.Toolkit.UI"
	  mc:Ignorable="d"
	  NavigationCacheMode="Required"
	  Background="{ThemeResource BackgroundBrush}">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<utu:NavigationBar Content="{Binding Title}" />

		<ScrollViewer Grid.Row="1"
					  VerticalScrollBarVisibility="Auto"
					  VerticalScrollMode="Auto"
					  HorizontalScrollBarVisibility="Disabled"
					  HorizontalScrollMode="Disabled">
			<Grid Padding="24,8">
				<Border Background="{ThemeResource SurfaceBrush}"
						CornerRadius="10"
						Padding="24"
						Margin="0,8"
						MaxWidth="480"
						MinWidth="320"
						HorizontalAlignment="Center"
						VerticalAlignment="Top">
					<StackPanel Spacing="16">
                        <Image Source="{Binding LogoImageUri}"
							   Width="72"
							   Height="72"
							   Stretch="Uniform"
							   HorizontalAlignment="Center" />
                        <TextBox Header="API Base URL"
							 Text="{Binding BaseUrl, Mode=TwoWay}"
							 PlaceholderText="http://127.0.0.1:8089"
							 HorizontalAlignment="Stretch" />
						<TextBox Header="Benutzername"
							 Text="{Binding Username, Mode=TwoWay}"
							 PlaceholderText="support"
							 HorizontalAlignment="Stretch" />
						<PasswordBox Header="Passwort"
							 PasswordRevealMode="Peek"
							 Password="{Binding Password, Mode=TwoWay}"
							 HorizontalAlignment="Stretch" />
						<CheckBox Content="Zugangsdaten speichern"
							  IsChecked="{Binding RememberLogin, Mode=TwoWay}" />
						<ProgressRing IsActive="{Binding IsBusy}"
								  Width="24"
								  Height="24"
								  HorizontalAlignment="Center" />
						<Button Content="Login"
								Command="{Binding Login}"
								HorizontalAlignment="Stretch"
								Padding="12,10" />
					</StackPanel>
				</Border>
			</Grid>
		</ScrollViewer>

		<Border Grid.Row="2"
				Background="{ThemeResource SurfaceVariantColor}"
				BorderBrush="{ThemeResource OutlineVariantColor}"
				BorderThickness="0,1,0,0"
				Padding="14,8">
			<TextBlock Text="{Binding Status}"
					   FontSize="11"
					   Opacity="0.8"
					   MaxLines="3"
					   TextWrapping="Wrap"
					   TextTrimming="CharacterEllipsis" />
		</Border>
	</Grid>
</Page>